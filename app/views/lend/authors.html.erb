<%= render 'shared/page_title', title: "Listing all authors" %>
<% @filter.each do |filter| %>
    <div class="well col-md-offset-4 col-md-4">
      <h4><center><%=link_to filter, lend_authorshelf_url(author: filter) %></center></h4>
    </div>
<% end %>
